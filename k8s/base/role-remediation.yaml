apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kubeagent-remediation
  namespace: kubeagent
rules:
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "patch"]
